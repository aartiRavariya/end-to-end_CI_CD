apiVersion: v1
kind: Service
metadata:
  name: streamlit-service
  namespace: ml-project
  labels:
    app: streamlit
spec:
  type: ClusterIP
  selector:
    app: streamlit
  ports:
  - name: http
    port: 8501
    targetPort: 8501
    protocol: TCP
